# Copy this file to .env and fill in your local values.
# Never commit .env - it contains secrets.

# Main backend API URL (Vite exposes only VITE_* vars to the client)
VITE_BACKEND_URL=http://localhost:5000